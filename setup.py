from setuptools import setup
setup(name='scout',
      version='0.1',
      description='Explore your time series data to gather insights for further work',
      packages=['scout'],
      zip_safe=False)
